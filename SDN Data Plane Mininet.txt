┌──────────────────────────┐
│   SDN Data Plane         │
│  (Mininet / Switches)    │
└───────────┬──────────────┘
            │
                    ▼
┌──────────────────────────┐
│ Feature Extraction Layer │
│ (1s Time Window, Stats)  │
└───────────┬──────────────┘
            │
                    ▼
┌──────────────────────────┐
│ CNN – Fast Sensor        │  ← ALWAYS RUNS
│ - Traffic morphology     │
│ - Burst detection        │
│ - Anomaly score          │
└───────────┬──────────────┘
            │
     ┌──────┴─────────┐
     │ Decision Gate  │
     │ (Confidence)   │
     └──────┬─────────┘
            │
   ┌────────┴─────────┐
   │                  │
     ▼                             ▼
┌──────────────┐   ┌──────────────┐
│ Transformer  │   │ GNN           │
│ Temporal     │   │ Spatial       │
│ Reasoner     │   │ Reasoner      │
│ (History)    │   │ (Topology)    │
└──────┬───────┘   └──────┬───────┘
       │                  │
       └────────┬─────────┘
                          ▼
     ┌──────────────────────────┐
     │ Fusion / Decision Engine │
     │ - Final class            │
     │ - Confidence             │
     └──────────────────────────┘



SDN Switches (Mininet)
        ↓
Feature Extraction (1s stats)
        ↓
Sliding Window Builder (2s CNN window)
        ↓
Stage 1: CNN – Fast Morphology Sensor (ALWAYS RUNS)
        ↓
Decision Gate (Confidence + anomaly trigger)
        ↓
        ├── Transformer (Temporal reasoning)
        └── GNN (Spatial reasoning)
        ↓
Fusion Engine
        ↓
Final Class + Confidence
        ↓
Mitigation Module

```
SELECTED_FEATURES = [

🧾 Traffic Volume

total_packets

total_bytes

packets_per_sec

bytes_per_sec

🌐 Flow Behavior

new_flow_rate

unique_src_ip

unique_dst_ip

entropy_src_ip

entropy_dst_port

flow_add_rate

flow_delete_rate

flow_churn_rate

📦 Control Plane

packet_in_rate

flow_mod_rate

table_miss_rate

packet_in_ratio

controller_cpu

control_channel_bw

🗃 Switch State

flow_table_occupancy

flow_table_utilization_ratio

timeout_events

🧠 Temporal (for CNN to embed)

rate_delta

burst_score

inter_arrival_variance

🗺 Spatial (per switch for GNN)

packet_in_rate_per_switch

table_utilization_per_switch

flow_add_rate_per_switch
<<<<<<< HEAD
```
=======
>>>>>>> b060148 (CNN and Transformer)

🏆 If You Implement These Correctly

Your IDS will:

Detect volumetric DDoS (controller flood)

Detect stealth DDoS (LOFT)

Detect switch exhaustion

Detect ARP poisoning

Detect control-plane abuse

And your architecture (CNN + Transformer + GNN) will actually make sense academically.
]



OPTIONAL_FEATURES = [
    "flow_count",  #controller workload
    "unique_src_ip",  #spoofing & distribution
    "unique_dst_ip",  #scanning & amplification
    "packet_in_count"  #direct controller stress
]



| Attack Type            | Covered by                |
| ---------------------- | ------------------------- |
| SYN / UDP / ICMP Flood | rate + dispersion         |
| Packet_in Flood        | rate_pkt_in               |
| LOFT                   | disp_interval + avg_durat |
| Flow Table Exhaustion  | avg_flow_dst + avg_durat  |
| Distributed DDoS       | switch_id + rate          |
| Topology Poisoning     | distribution anomalies    |
| NB / SB abuse          | rate + flow_count         |

